{
  "name": "tiketkuy-root",
  "version": "1.0.0",
  "scripts": {
    "dev:backend": "npm start --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "test:db": "node backend/test.js"
  }
}
